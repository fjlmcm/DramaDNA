[advisories]
# `rsa` is an optional dependency of sqlx-mysql. DramaDNA disables sqlx default
# features and enables only SQLite; `cargo tree --target all -i rsa@0.9.10`
# confirms that the crate is not present in any application build graph.
ignore = ["RUSTSEC-2023-0071"]
